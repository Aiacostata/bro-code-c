#include <stdio.h>

int main() {
  // Operadores Aritméticos = + - * / % ++ --

  int x = 20;
  int y = 6;
  int z = 0;
  int a = 0;

  int b = 0;

  // z = x + y;
  // z = x - y;
  // z = y - x;
  // z = x * y;
  // w = x / y;

  // a = x / y;
  // z = x % y;
  //  b++;
  //++b;
  //  b--;
  --b;

  z += x;

  printf("%d\n", z);
  // printf("%d\n", a);

  printf("%d\n", b);
  return 0;
}
